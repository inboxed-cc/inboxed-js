{
  "name": "@inboxedcc/inboxed-js",
  "version": "0.0.1",
  "description": "Inboxed API client for javascript and typescript",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "release": "pnpm run build && changeset publish",
    "lint": "tsc",
    "build": "tsup src/index.ts --format cjs,esm --dts"
  },
  "license": "MIT",
  "publishConfig": {
    "@inboxedcc:registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
